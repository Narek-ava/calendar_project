<tr>
    <td class="header">
        <a href="{{ $url }}" style="display: inline-block;">
            <img
                class="logo"
                alt="{{ $logoAlt ?? 'Chilled Butter App' }}"
                src="{{ Storage::url($logo ?? 'images/cb_horizontal_title.png') }}"
                width="128"
            />
        </a>
    </td>
</tr>
