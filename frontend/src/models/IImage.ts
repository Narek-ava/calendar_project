export interface IImage {
    link: string;
}

export interface ImageResponse {
    link: string;
    preview: string;
}

export interface ImageData {
    id?: number;
    url: string;
    link: string;
    filename?: string;
}
